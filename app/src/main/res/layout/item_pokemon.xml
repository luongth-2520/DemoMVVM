<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.example.demomvvm.screen.adapter.ItemPokemonViewModel" />
    </data>

    <androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/dp_5"
        android:onClick="@{viewModel::onItemClicked}">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <com.example.demomvvm.utils.customview.CircleImageView
                android:id="@+id/imagePokemon"
                android:layout_width="@dimen/dp_120"
                android:layout_height="@dimen/dp_120"
                android:src="@drawable/ic_launcher_background"
                app:imageUrl="@{viewModel.pokemon.imageUrl}"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/textPokemonName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{viewModel.pokemon.name}"
                app:layout_constraintBottom_toTopOf="@id/imageFavorite"
                app:layout_constraintEnd_toEndOf="@id/imagePokemon"
                app:layout_constraintStart_toStartOf="@id/imagePokemon"
                app:layout_constraintTop_toBottomOf="@id/imagePokemon"
                tools:text="Pokemon name" />

            <ImageView
                android:id="@+id/imageFavorite"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_favorite_border"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="@id/textPokemonName"
                app:layout_constraintStart_toStartOf="@id/textPokemonName" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>
